import { test, expect } from "@playwright/test";
import { MainPage } from "../pages/main-page";

test.describe("Обзор элементов Главной страницы", async () => {

  test('Проверка видимости всех блоков содержащих текст', async ({ page }) => {
    test.skip();
  });

  test('Проверка видимости всех заголовков с текстом', async ({ page }) => {
    test.skip();
  });

  test('Скролл к блоку в середине, проверка нахождения блока в ViewPort', async ({ page }) => {
    test.skip();
  });
});
